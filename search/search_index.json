{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Odoo Developer Kit (ODK)","text":"<p>Odoo Developer Kit (<code>odk</code>) is a Rust CLI for Odoo developers. It helps teams diagnose local development machines and generate clean, consistent Odoo development projects.</p> <p>ODK is designed with a small and focused command surface:</p> <pre><code>odk doctor\nodk create\nodk upgrade\n</code></pre>"},{"location":"#what-is-odk","title":"What Is ODK?","text":"<p>ODK is a developer toolkit for building professional Odoo development environments.</p> <p>It helps Odoo developers:</p> <ul> <li>Check required local tools.</li> <li>Generate clean Odoo project scaffolding.</li> <li>Create a Python environment using <code>uv</code>.</li> <li>Generate Docker development files.</li> <li>Generate PyCharm configuration.</li> <li>Generate VS Code configuration.</li> <li>Standardize project setup across a team.</li> </ul> <p>The goal is simple:</p> <p>Run <code>odk doctor</code>, fix your machine, then run <code>odk create</code>.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Environment diagnostics for Odoo development tools</li> <li>Interactive Odoo project generator</li> <li><code>uv</code> based Python environment creation</li> <li>Docker development setup</li> <li>PyCharm run configuration generation</li> <li>VS Code launch configuration generation</li> <li>Cross-platform support for macOS, Linux, and Windows</li> <li>GitHub Release installers with <code>cargo-dist</code></li> <li>Clean generated project folder without ODK source files</li> </ul>"},{"location":"#installation","title":"Installation","text":""},{"location":"#install-from-github-release","title":"Install from GitHub Release","text":""},{"location":"#macos-linux","title":"macOS / Linux","text":"<pre><code>curl --proto '=https' --tlsv1.2 -LsSf https://github.com/HanZawNyein/odoo_developer_kit/releases/latest/download/odoo-developer-kit-installer.sh | sh\n</code></pre>"},{"location":"#windows-powershell","title":"Windows PowerShell","text":"<pre><code>powershell -ExecutionPolicy Bypass -c \"irm https://github.com/HanZawNyein/odoo_developer_kit/releases/latest/download/odoo-developer-kit-installer.ps1 | iex\"\n</code></pre> <p>After installation, verify:</p> <pre><code>odk doctor\n</code></pre> <p>If <code>odk</code> is not found after installation, add Cargo bin to your PATH.</p> <p>For macOS or Linux:</p> <pre><code>echo 'export PATH=\"$HOME/.cargo/bin:$PATH\"' &gt;&gt; ~/.zshrc\nsource ~/.zshrc\n</code></pre> <p>For bash:</p> <pre><code>echo 'export PATH=\"$HOME/.cargo/bin:$PATH\"' &gt;&gt; ~/.bashrc\nsource ~/.bashrc\n</code></pre>"},{"location":"#install-from-github-source","title":"Install from GitHub Source","text":"<pre><code>cargo install --git https://github.com/HanZawNyein/odoo_developer_kit.git --locked\n</code></pre> <p>Verify:</p> <pre><code>odk doctor\n</code></pre>"},{"location":"#install-from-local-checkout","title":"Install from Local Checkout","text":"<pre><code>git clone https://github.com/HanZawNyein/odoo_developer_kit.git\ncd odoo_developer_kit\ncargo install --path . --locked\n</code></pre> <p>Verify:</p> <pre><code>odk doctor\n</code></pre>"},{"location":"#usage","title":"Usage","text":""},{"location":"#check-your-machine","title":"Check your machine","text":"<pre><code>odk doctor\n</code></pre> <p>Example output:</p> <pre><code>Odoo Developer Kit Doctor\n\n[\u2713] uv\n    Version: 0.11.21\n\n[\u2713] Docker\n    Version: 28.3.2\n\n[\u2713] PyCharm\n    Version: 2026.1.4\n\n[\u2713] VS Code\n    Version: 1.127.0\n\n[\u2713] PostgreSQL\n    Version: 18.4\n\n[\u2713] wkhtmltopdf\n    Version: 0.12.6\n\nEnvironment ready!\n</code></pre>"},{"location":"#create-a-project","title":"Create a project","text":"<pre><code>odk create\n</code></pre> <p>Example interactive flow:</p> <pre><code>Odoo Project Creator\n\nProject Name:\n&gt; sample\n\nProject Path:\n  sample (default)\n&gt; /Users/agga/Documents/python-dev/odoo-dev\n\nGit Repository:\n  leave empty to skip\n&gt; \n\nOdoo Source Code Path:\n&gt; /Users/agga/src/odoo\n\nOdoo Version:\n  19.1 (detected)\n\nPython Version:\n  3.8\n  3.9\n  3.10\n  3.11\n  3.12\n  3.13\n&gt; 3.10\n\nUse Docker:\n  yes/no\n&gt; yes\n\nPostgreSQL Version:\n  17 (default)\n  16\n&gt; 17\n\nGenerate PyCharm:\n  yes/no\n&gt; yes\n\nGenerate VS Code:\n  yes/no\n&gt; yes\n</code></pre> <p>The project path is the parent directory, so this example creates <code>/Users/agga/Documents/python-dev/odoo-dev/sample</code>.</p> <p>ODK then:</p> <ol> <li>Creates a clean project directory.</li> <li>Installs the selected Python version with <code>uv</code>.</li> <li>Creates <code>.venv</code> using <code>uv venv</code>.</li> <li>Installs <code>&lt;odoo_source_path&gt;/requirements.txt</code> with <code>uv pip install -r</code>.</li> <li>Generates Odoo project files from templates.</li> <li>Generates Docker files when selected, using <code>major.0</code> as the Odoo image tag.</li> <li>Generates PyCharm configuration when selected.</li> <li>Generates VS Code configuration when selected.</li> <li>Runs <code>uv lock</code>.</li> </ol> <p>ODK does not use <code>python -m venv</code>.</p>"},{"location":"#commands","title":"Commands","text":"Command Purpose Best time to run <code>odk doctor</code> Validate the local development environment Before onboarding, after OS upgrades, or before starting a new project <code>odk create</code> Generate a clean Odoo development project When bootstrapping a new Odoo development environment <code>odk upgrade</code> Install the latest ODK release When <code>odk doctor</code> reports that a newer version is available"},{"location":"#odk-doctor","title":"<code>odk doctor</code>","text":"<p><code>odk doctor</code> checks the tools commonly needed for Odoo development.</p> <p>Current checks:</p> <ul> <li><code>uv</code></li> <li>Docker</li> <li>PyCharm</li> <li>VS Code</li> <li>PostgreSQL</li> <li><code>wkhtmltopdf</code></li> </ul> <p>Example:</p> <pre><code>odk doctor\n</code></pre> <p>Output:</p> <pre><code>Odoo Developer Kit Doctor\n\n[\u2713] uv\n    Version: 0.11.21\n\n[\u2713] Docker\n    Version: 28.3.2\n\n[\u2713] PyCharm\n    Version: 2026.1.4\n\n[\u2713] VS Code\n    Version: 1.127.0\n\n[\u2713] PostgreSQL\n    Version: 18.4\n\n[\u2713] wkhtmltopdf\n    Version: 0.12.6\n\nEnvironment ready!\n</code></pre> <p>If a tool is missing, ODK shows an installation suggestion for your operating system.</p> <p>If a newer ODK release is available, <code>odk doctor</code> prints a warning and suggests <code>odk upgrade</code>.</p>"},{"location":"#odk-create","title":"<code>odk create</code>","text":"<p><code>odk create</code> starts an interactive Odoo project generator.</p> <p>It also accepts common values as flags:</p> <pre><code>odk create --project-name burma --project-path=/Users/agga/Documents/python-dev/odoo-dev --docker --psql=17 --pycharm --vscode --doctor\n</code></pre> <p><code>--doctor</code> runs <code>odk doctor</code> first. Missing required values are prompted. In flag mode, omitted boolean flags such as <code>--docker</code> default to disabled. <code>--postgres-version</code> and <code>--psql</code> are only valid with <code>--docker</code>. <code>--projec-path</code> is accepted as an alias for <code>--project-path</code>.</p> <p>It asks for:</p> <ul> <li>Project name</li> <li>Project path</li> <li>Optional Git repository</li> <li>Odoo version</li> <li>Python version</li> <li>Docker usage</li> <li>PostgreSQL version</li> <li>PyCharm configuration</li> <li>VS Code configuration</li> </ul> <p>Supported Odoo versions:</p> <pre><code>19.1\n19.0\n18.1\n18.0\n17.0\n</code></pre> <p>Supported Python versions:</p> <pre><code>3.8\n3.9\n3.10\n3.11\n3.12\n3.13\n</code></pre> <p>Supported PostgreSQL versions for Docker:</p> <pre><code>17\n16\n</code></pre>"},{"location":"#generated-project-structure","title":"Generated Project Structure","text":"<p>When Docker, PyCharm, and VS Code are enabled, a generated project can look like this:</p> <pre><code>project/\n\u251c\u2500\u2500 .venv/\n\u251c\u2500\u2500 addons/\n\u251c\u2500\u2500 config/\n\u2502   \u2514\u2500\u2500 odoo.conf\n\u251c\u2500\u2500 .idea/\n\u251c\u2500\u2500 .vscode/\n\u251c\u2500\u2500 compose.yaml\n\u251c\u2500\u2500 Dockerfile\n\u251c\u2500\u2500 odoo.conf\n\u251c\u2500\u2500 .gitignore\n\u251c\u2500\u2500 odoo_pg_pass\n\u251c\u2500\u2500 README.md\n\u251c\u2500\u2500 pyproject.toml\n\u2514\u2500\u2500 uv.lock\n</code></pre> <p>When Docker is disabled, ODK can generate:</p> <pre><code>project/\n\u251c\u2500\u2500 .venv/\n\u251c\u2500\u2500 addons/\n\u251c\u2500\u2500 .idea/\n\u251c\u2500\u2500 .vscode/\n\u251c\u2500\u2500 odoo.conf\n\u251c\u2500\u2500 .gitignore\n\u251c\u2500\u2500 README.md\n\u251c\u2500\u2500 pyproject.toml\n\u2514\u2500\u2500 uv.lock\n</code></pre> <p>Depending on your selections, <code>.idea/</code>, <code>.vscode/</code>, and Docker files may not be generated.</p>"},{"location":"#files-that-should-not-be-generated","title":"Files That Should Not Be Generated","text":"<p>ODK should generate a clean Odoo project only.</p> <p>The generated project should not include ODK source repository files such as:</p> <pre><code>.github/\nCargo.lock\nCargo.toml\ndocs/\nsrc/\ntemplates/\ntests/\ninstall.sh\nmkdocs.yml\n</code></pre> <p>If these files appear inside a generated Odoo project, it usually means the ODK repository itself was cloned into the target folder instead of generating a clean project scaffold.</p>"},{"location":"#docker-development","title":"Docker Development","text":"<p>When Docker is enabled, ODK generates:</p> <pre><code>compose.yaml\nDockerfile\nconfig/odoo.conf\nodoo.conf\nodoo_pg_pass\n</code></pre> <p>The default Docker setup includes:</p> <ul> <li>Odoo service</li> <li>PostgreSQL service</li> <li>Docker volumes</li> <li>PostgreSQL password secret</li> <li>Custom addons volume</li> </ul> <p>Start the generated Docker project:</p> <pre><code>docker compose up -d\n</code></pre> <p>Stop it:</p> <pre><code>docker compose down\n</code></pre> <p>Open Odoo:</p> <pre><code>http://localhost:8069\n</code></pre>"},{"location":"#pycharm-support","title":"PyCharm Support","text":"<p>When PyCharm generation is enabled, ODK creates <code>.idea</code> configuration files.</p> <p>The generated PyCharm run configuration uses the generated <code>uv</code> environment and points to the Odoo source path entered during project creation. It runs:</p> <pre><code>&lt;Odoo Source Code Path&gt;/odoo-bin -c $PROJECT_DIR$/odoo.conf\n</code></pre> <p>ODK also generates the project module, VCS mapping, inspection profile, Black SDK, and shared run configuration. Session-specific <code>workspace.xml</code> remains ignored and is created by PyCharm when needed.</p>"},{"location":"#vs-code-support","title":"VS Code Support","text":"<p>When VS Code generation is enabled, ODK creates:</p> <pre><code>.vscode/settings.json\n.vscode/launch.json\n</code></pre> <p>The generated launch configuration runs:</p> <pre><code>${workspaceFolder}/odoo-bin -c odoo.conf\n</code></pre> <p>This works best when your project layout has <code>odoo-bin</code> at the project root.</p>"},{"location":"#development","title":"Development","text":"<p>Clone the repository:</p> <pre><code>git clone https://github.com/HanZawNyein/odoo_developer_kit.git\ncd odoo_developer_kit\n</code></pre> <p>Run tests:</p> <pre><code>cargo test --all-features\n</code></pre> <p>Run formatting check:</p> <pre><code>cargo fmt --all -- --check\n</code></pre> <p>Run clippy:</p> <pre><code>cargo clippy --all-targets --all-features -- -D warnings\n</code></pre> <p>Run all checks:</p> <pre><code>cargo fmt --all -- --check\ncargo clippy --all-targets --all-features -- -D warnings\ncargo test --all-features\n</code></pre> <p>Run locally:</p> <pre><code>cargo run -- doctor\ncargo run -- create\n</code></pre> <p>Install locally:</p> <pre><code>cargo install --path . --locked --force\n</code></pre>"},{"location":"#release","title":"Release","text":"<p>ODK uses <code>cargo-dist</code> for release builds and GitHub Release installers.</p> <p>Check release plan:</p> <pre><code>dist plan --tag v0.1.1\n</code></pre> <p>Example output:</p> <pre><code>announcing v0.1.1\n  odoo-developer-kit 0.1.1\n    source.tar.gz\n    odoo-developer-kit-installer.sh\n    odoo-developer-kit-installer.ps1\n    sha256.sum\n    odoo-developer-kit-aarch64-apple-darwin.tar.xz\n    odoo-developer-kit-aarch64-unknown-linux-gnu.tar.xz\n    odoo-developer-kit-x86_64-apple-darwin.tar.xz\n    odoo-developer-kit-x86_64-pc-windows-msvc.zip\n    odoo-developer-kit-x86_64-unknown-linux-gnu.tar.xz\n</code></pre> <p>To publish a release, make sure the version in <code>Cargo.toml</code> matches the Git tag.</p> <p>Example:</p> <pre><code>version = \"0.1.1\"\n</code></pre> <p>Then tag:</p> <pre><code>git tag v0.1.1\ngit push origin v0.1.1\n</code></pre> <p>Important rule:</p> <pre><code>Cargo.toml version = 0.1.1\nGit tag            = v0.1.1\n</code></pre> <p>If the version and tag do not match, <code>dist</code> will fail with:</p> <pre><code>This workspace doesn't have anything for dist to Release!\n</code></pre>"},{"location":"#troubleshooting","title":"Troubleshooting","text":""},{"location":"#cargo-dist-init-says-no-such-command","title":"<code>cargo dist init</code> says no such command","text":"<p>If this happens:</p> <pre><code>error: no such command: `dist`\n</code></pre> <p>Check if <code>dist</code> exists:</p> <pre><code>ls ~/.cargo/bin | grep dist\n</code></pre> <p>If the binary is installed as <code>dist</code>, run:</p> <pre><code>~/.cargo/bin/dist --version\n~/.cargo/bin/dist init\n</code></pre> <p>If you want to use it as <code>cargo dist</code>, create a symlink:</p> <pre><code>ln -s ~/.cargo/bin/dist ~/.cargo/bin/cargo-dist\n</code></pre> <p>Then check:</p> <pre><code>cargo dist --version\n</code></pre>"},{"location":"#github-release-installer-returns-404","title":"GitHub Release installer returns 404","text":"<p>If this command returns 404:</p> <pre><code>curl --proto '=https' --tlsv1.2 -LsSf https://github.com/HanZawNyein/odoo_developer_kit/releases/latest/download/odoo-developer-kit-installer.sh | sh\n</code></pre> <p>It means the installer asset does not exist in the latest GitHub Release yet.</p> <p>Check:</p> <pre><code>GitHub Repo \u2192 Releases \u2192 Latest release \u2192 Assets\n</code></pre> <p>The release should contain:</p> <pre><code>odoo-developer-kit-installer.sh\nodoo-developer-kit-installer.ps1\n</code></pre> <p>If only <code>.zip</code> or <code>.tar.xz</code> files exist, the release workflow did not generate global installer artifacts.</p>"},{"location":"#odk-command-not-found","title":"<code>odk</code> command not found","text":"<p>Add Cargo bin to PATH:</p> <pre><code>echo 'export PATH=\"$HOME/.cargo/bin:$PATH\"' &gt;&gt; ~/.zshrc\nsource ~/.zshrc\n</code></pre> <p>Then check:</p> <pre><code>odk doctor\n</code></pre>"},{"location":"#project-status","title":"Project Status","text":"<p>ODK currently focuses on two commands:</p> <pre><code>odk doctor\nodk create\n</code></pre> <p>Future ideas:</p> <ul> <li><code>odk addon create</code></li> <li><code>odk run</code></li> <li><code>odk update</code></li> <li><code>odk test</code></li> <li>non-interactive <code>odk create</code> flags</li> <li>improved Docker templates</li> <li>Odoo source path support</li> <li>enterprise addons path support</li> </ul>"},{"location":"#license","title":"License","text":"<p>This project is licensed under the Apache-2.0 License.</p> <p>See LICENSE for details.</p>"},{"location":"#author","title":"Author","text":"<p>Created by Han Zaw Nyein.</p> <p>Repository:</p> <pre><code>https://github.com/HanZawNyein/odoo_developer_kit\n</code></pre> <p>Documentation:</p> <pre><code>https://hanzawnyein.github.io/odoo_developer_kit/\n</code></pre>"},{"location":"create/","title":"<code>odk create</code>","text":"<p><code>odk create</code> is an interactive project generator for Odoo development repositories.</p> <pre><code>odk create\n</code></pre> <p>You can also pass common answers as flags:</p> <pre><code>odk create --project-name burma --project-path=/Users/agga/Documents/python-dev/odoo-dev --docker --psql=17 --pycharm --vscode --doctor\n</code></pre> <p><code>--doctor</code> runs environment checks before creation. Missing required values are prompted. In flag mode, omitted boolean flags such as <code>--docker</code> default to disabled. <code>--postgres-version</code> and <code>--psql</code> are only valid with <code>--docker</code>. <code>--projec-path</code> is accepted as an alias for <code>--project-path</code>.</p>"},{"location":"create/#interactive-flow","title":"Interactive Flow","text":"<p>The generator asks for the values that shape the project:</p> <ol> <li>Project Name</li> <li>Project Path</li> <li>Git Repository, optional</li> <li>Odoo Source Code Path</li> <li>Odoo Version, detected from <code>odoo/release.py</code> when available, including minor versions like <code>18.1</code></li> <li>Python Version</li> <li>PostgreSQL Version</li> <li>Use Docker</li> <li>Generate PyCharm</li> <li>Generate VS Code</li> </ol> <pre><code>Odoo Project Creator\n\nProject Name:\n&gt; geaai_odoo\n\nProject Path:\n  geaai_odoo (default)\n&gt; /Users/agga/Documents/python-dev/odoo-dev\n\nGit Repository:\n  leave empty to skip\n&gt; \n\nOdoo Source Code Path:\n&gt; /Users/agga/src/odoo\n\nOdoo Version:\n  19.1 (detected)\n\nPython Version:\n  3.10\n  3.12\n  3.11\n  3.13\n&gt; 3.10\n</code></pre> <p><code>Project Path</code> accepts the parent directory. For the values above, the generated project directory is <code>/Users/agga/Documents/python-dev/odoo-dev/geaai_odoo</code>. Entering a complete path that already ends with the project name is also supported.</p>"},{"location":"create/#workflow","title":"Workflow","text":"<p>ODK performs the setup in a deterministic order:</p> <pre><code>flowchart LR\n    A[Collect answers] --&gt; B[Validate Python compatibility]\n    B --&gt; C[Clone repository when provided]\n    C --&gt; D[Install Python with uv]\n    D --&gt; E[Create .venv with uv]\n    E --&gt; F[Install Odoo requirements]\n    F --&gt; G[Render Tera templates]\n    G --&gt; H[Generate uv.lock]</code></pre> <p>The environment is always created with <code>uv</code>:</p> <pre><code>uv python install &lt;version&gt;\nuv venv .venv --python &lt;version&gt;\nuv pip install -r &lt;odoo_source_path&gt;/requirements.txt\n</code></pre> <p>Requirement installation failures are reported but do not stop project creation.</p> <p>When Docker is enabled, ODK derives the image tag from the detected Odoo version's major number. For example, Odoo <code>18.1</code> renders <code>FROM odoo:18.0</code>.</p> <p>No <code>python -m venv</code></p> <p>ODK deliberately avoids <code>python -m venv</code> so teams get one consistent Python environment workflow.</p>"},{"location":"create/#compatibility-matrix","title":"Compatibility Matrix","text":"Odoo Version Supported Python Versions 19.1 3.12, 3.13 19.0 3.12, 3.13 18.1 3.11, 3.12 18.0 3.11, 3.12 17.0 3.11 <p>Invalid combinations fail before cloning or generating files.</p>"},{"location":"create/#generated-project-structure","title":"Generated Project Structure","text":"<pre><code>project/\n\u251c\u2500\u2500 .venv/\n\u251c\u2500\u2500 addons/\n\u251c\u2500\u2500 custom/\n\u251c\u2500\u2500 docker/\n\u251c\u2500\u2500 scripts/\n\u251c\u2500\u2500 .idea/\n\u251c\u2500\u2500 .vscode/\n\u251c\u2500\u2500 compose.yaml\n\u251c\u2500\u2500 Dockerfile\n\u251c\u2500\u2500 odoo.conf\n\u251c\u2500\u2500 .gitignore\n\u251c\u2500\u2500 README.md\n\u251c\u2500\u2500 pyproject.toml\n\u2514\u2500\u2500 uv.lock\n</code></pre>"},{"location":"create/#template-variables","title":"Template Variables","text":"<p>Every generated file is rendered with Tera. Templates receive:</p> Variable Description <code>project_name</code> Project directory and package name <code>odoo_version</code> Selected Odoo version <code>docker_odoo_version</code> Stable Docker image tag derived from <code>odoo_version</code>, for example <code>18.0</code> for <code>18.1</code> <code>python_version</code> Selected Python version <code>postgres_version</code> Selected PostgreSQL version <code>database_name</code> PostgreSQL-safe database name derived from the project name"},{"location":"create/#editor-configuration","title":"Editor Configuration","text":"<p>When selected, ODK generates editor configuration that points to <code>.venv</code>.</p> PyCharmVS Code <pre><code>Script: &lt;Odoo Source Code Path&gt;/odoo-bin\nArguments: -c $PROJECT_DIR$/odoo.conf\nInterpreter: uv (&lt;Project Name&gt;)\n</code></pre> <pre><code>{\n  \"python.defaultInterpreterPath\": \"${workspaceFolder}/.venv/bin/python\"\n}\n</code></pre>"},{"location":"development/","title":"Development","text":"<p>Clone the repository:</p> <pre><code>git clone https://github.com/HanZawNyein/odoo_developer_kit.git\ncd odoo_developer_kit\n</code></pre> <p>Run tests:</p> <pre><code>cargo test\n</code></pre> <p>Run formatting and lint checks:</p> <pre><code>cargo fmt --all -- --check\ncargo clippy --all-targets --all-features -- -D warnings\n</code></pre> <p>Run locally:</p> <pre><code>cargo run -- doctor\ncargo run -- create\n</code></pre>"},{"location":"docker/","title":"Docker Development","text":"<p>When Docker is enabled, ODK generates:</p> <pre><code>compose.yaml\nDockerfile\nconfig/odoo.conf\nodoo.conf\nodoo_pg_pass\n</code></pre> <p>Start the generated Docker project:</p> <pre><code>docker compose up -d\n</code></pre> <p>Stop it:</p> <pre><code>docker compose down\n</code></pre> <p>Open Odoo:</p> <pre><code>http://localhost:8069\n</code></pre> <p>Docker uses <code>config/odoo.conf</code>. Local editor runs use root <code>odoo.conf</code>.</p>"},{"location":"doctor/","title":"<code>odk doctor</code>","text":"<p><code>odk doctor</code> validates the local Odoo developer environment and prints friendly, actionable output.</p> <pre><code>odk doctor\n</code></pre>"},{"location":"doctor/#what-it-checks","title":"What It Checks","text":"<p>ODK checks only the tools that are part of the supported Odoo workflow.</p> Tool Detection <code>uv</code> <code>uv --version</code> Docker <code>docker --version</code> Python <code>python --version</code>, then <code>python3 --version</code> PyCharm platform-specific app or command detection VS Code <code>code --version</code> PostgreSQL <code>psql --version</code> <code>wkhtmltopdf</code> <code>wkhtmltopdf --version</code> <p>It also checks whether the running ODK binary is behind the latest GitHub Release. If a newer version is available, it prints a warning and suggests <code>odk upgrade</code>.</p>"},{"location":"doctor/#example-ready-environment","title":"Example: Ready Environment","text":"<pre><code>Odoo Developer Kit Doctor\n\n[\u2713] uv\n    Version: 0.8.4\n\n[\u2713] Docker\n    Version: 28.3.2\n\n[\u2713] Python\n    Version: 3.13.5\n\n[\u2713] PyCharm\n    Version: 2026.1\n\n[\u2713] VS Code\n    Version: 1.104\n\n[\u2713] PostgreSQL\n    Version: 17\n\n[\u2713] wkhtmltopdf\n    Version: 0.12.6\n\nEnvironment ready!\n</code></pre>"},{"location":"doctor/#example-missing-tool","title":"Example: Missing Tool","text":"<pre><code>[\u2717] wkhtmltopdf\n    Not installed\n\nSuggestion:\n    brew install wkhtmltopdf\n</code></pre> <p>No hard crash</p> <p>Missing commands are handled as normal diagnostic results. ODK never panics because a tool is absent from the machine.</p>"},{"location":"doctor/#pycharm-detection","title":"PyCharm Detection","text":"<p>PyCharm requires platform-specific handling:</p> <ul> <li>macOS scans <code>/Applications/PyCharm*.app</code> and <code>~/Applications/PyCharm*.app</code></li> <li>Linux checks the <code>pycharm</code> command and <code>which pycharm</code></li> <li>Windows scans common JetBrains locations under Program Files</li> </ul>"},{"location":"doctor/#supported-platforms","title":"Supported Platforms","text":"OS Status macOS Supported Linux Supported Windows Supported"},{"location":"editors/","title":"Editor Support","text":"<p>ODK can generate PyCharm and VS Code configuration during <code>odk create</code>.</p>"},{"location":"editors/#pycharm","title":"PyCharm","text":"<p>The generated PyCharm run configuration points to:</p> <pre><code>&lt;Odoo Source Code Path&gt;/odoo-bin -c $PROJECT_DIR$/odoo.conf\n</code></pre> <p>It uses the generated <code>uv</code> environment, adds the external Odoo source as a module content root, and generates the module, VCS, inspection, and shared run configuration files under <code>.idea/</code>. PyCharm creates its own ignored <code>workspace.xml</code> when the project is opened.</p>"},{"location":"editors/#vs-code","title":"VS Code","text":"<p>The generated VS Code configuration points to:</p> <pre><code>${workspaceFolder}/.venv/bin/python\n${workspaceFolder}/odoo-bin -c odoo.conf\n</code></pre> <p>Both editors use root <code>odoo.conf</code> for local development.</p>"},{"location":"installation/","title":"Installation","text":"<p>ODK can be installed from a prebuilt release binary or from Cargo. The release installer is the recommended path for developers who just want the <code>odk</code> command.</p>"},{"location":"installation/#recommended-installer","title":"Recommended Installer","text":"<pre><code>curl -LsSf https://raw.githubusercontent.com/HanZawNyein/odoo_developer_kit/refs/heads/1.0/install.sh | sh\n</code></pre> <p>The installer:</p> <ul> <li>detects the operating system</li> <li>detects CPU architecture</li> <li>downloads the matching GitHub release asset</li> <li>installs <code>odk</code> into <code>~/.local/bin</code></li> <li>verifies the binary before exiting</li> </ul> <p>PATH</p> <p>If <code>~/.local/bin</code> is not already on your <code>PATH</code>, the installer prints the shell export line to add it.</p>"},{"location":"installation/#install-with-cargo","title":"Install With Cargo","text":"<p>Use Cargo when you already have a Rust toolchain installed:</p> <pre><code>cargo install odoo-developer-kit\n</code></pre> <p>Verify the command:</p> <pre><code>odk doctor\n</code></pre>"},{"location":"installation/#github-releases","title":"GitHub Releases","text":"<p>ODK publishes binaries for the platforms below.</p> Platform Architecture Target macOS ARM64 <code>aarch64-apple-darwin</code> macOS x64 <code>x86_64-apple-darwin</code> Linux x64 <code>x86_64-unknown-linux-gnu</code> Linux ARM64 <code>aarch64-unknown-linux-gnu</code> Windows x64 <code>x86_64-pc-windows-msvc</code> <p>Download the archive for your platform from GitHub Releases, extract the <code>odk</code> binary, and place it on your <code>PATH</code>.</p> <p>404 from the installer</p> <p>The installer downloads from GitHub Releases, not from the raw branch. If no release has been published yet, GitHub returns <code>404</code>.</p> <p>Create a version tag such as <code>v0.1.0</code> and let the release workflow publish the cargo-dist assets first. The macOS ARM64 asset should look like:</p> <pre><code>https://github.com/HanZawNyein/odoo_developer_kit/releases/latest/download/odoo-developer-kit-aarch64-apple-darwin.tar.xz\n</code></pre>"},{"location":"installation/#build-from-source","title":"Build From Source","text":"<pre><code>git clone https://github.com/HanZawNyein/odoo_developer_kit.git\ncd odoo_developer_kit\ncargo build --release\n</code></pre> <p>The release binary is written to:</p> macOS / LinuxWindows <pre><code>target/release/odk\n</code></pre> <pre><code>target\\release\\odk.exe\n</code></pre>"},{"location":"installation/#verify-installation","title":"Verify Installation","text":"<pre><code>odk doctor\n</code></pre> <p>If any required tool is missing, ODK prints a platform-specific installation suggestion.</p>"},{"location":"project-structure/","title":"Generated Project Structure","text":"<p>When Docker, PyCharm, and VS Code are enabled, a generated project can look like this:</p> <pre><code>project/\n\u251c\u2500\u2500 .venv/\n\u251c\u2500\u2500 addons/\n\u251c\u2500\u2500 config/\n\u2502   \u2514\u2500\u2500 odoo.conf\n\u251c\u2500\u2500 .idea/\n\u251c\u2500\u2500 .vscode/\n\u251c\u2500\u2500 compose.yaml\n\u251c\u2500\u2500 Dockerfile\n\u251c\u2500\u2500 odoo.conf\n\u251c\u2500\u2500 .gitignore\n\u251c\u2500\u2500 odoo_pg_pass\n\u251c\u2500\u2500 README.md\n\u251c\u2500\u2500 pyproject.toml\n\u2514\u2500\u2500 uv.lock\n</code></pre> <p>The root <code>odoo.conf</code> is for local runs. <code>config/odoo.conf</code> is for Docker.</p> <p>ODK does not generate <code>.env</code> or <code>.env.example</code>.</p>"},{"location":"publishing/","title":"Publishing Documentation","text":"<p>ODK uses Material for MkDocs for documentation. The current repository workflow publishes documentation on pushes to <code>1.0</code> and <code>main</code> with:</p> <pre><code>mkdocs gh-deploy --force\n</code></pre> <p>This keeps documentation changes reviewable before they are merged.</p>"},{"location":"publishing/#github-pages-url","title":"GitHub Pages URL","text":"<p>For this repository, GitHub Pages should publish to:</p> <pre><code>https://hanzawnyein.github.io/odoo_developer_kit/\n</code></pre> <p>The matching <code>mkdocs.yml</code> setting is:</p> <pre><code>site_url: https://hanzawnyein.github.io/odoo_developer_kit/\n</code></pre>"},{"location":"publishing/#active-github-pages-workflow","title":"Active GitHub Pages Workflow","text":"<p>The active workflow runs only on pushes:</p> <pre><code>name: Docs\n\non:\n  push:\n    branches:\n      - \"1.0\"\n      - main\n\npermissions:\n  contents: write\n\njobs:\n  deploy:\n    runs-on: ubuntu-latest\n\n    steps:\n      - name: Checkout repository\n        uses: actions/checkout@v4\n\n      - name: Setup Python\n        uses: actions/setup-python@v5\n        with:\n          python-version: \"3.x\"\n\n      - name: Cache MkDocs\n        uses: actions/cache@v4\n        with:\n          key: mkdocs-material-${{ github.run_id }}\n          path: ~/.cache\n          restore-keys: |\n            mkdocs-material-\n\n      - name: Install documentation dependencies\n        run: python -m pip install mkdocs-material\n\n      - name: Deploy documentation\n        run: mkdocs gh-deploy --force\n</code></pre>"},{"location":"publishing/#github-pages-publishing-example","title":"GitHub Pages Publishing Example","text":"<p>Material for MkDocs documents a simple GitHub Actions deployment flow using <code>mkdocs gh-deploy --force</code>. This is the same approach used by ODK:</p> <pre><code>name: Publish Docs\n\non:\n  push:\n    branches:\n      - main\n\npermissions:\n  contents: write\n\njobs:\n  deploy:\n    runs-on: ubuntu-latest\n\n    steps:\n      - name: Checkout repository\n        uses: actions/checkout@v4\n\n      - name: Configure Git credentials\n        run: |\n          git config user.name github-actions[bot]\n          git config user.email 41898282+github-actions[bot]@users.noreply.github.com\n\n      - name: Setup Python\n        uses: actions/setup-python@v5\n        with:\n          python-version: \"3.x\"\n\n      - name: Cache MkDocs\n        uses: actions/cache@v4\n        with:\n          key: mkdocs-material-${{ github.run_id }}\n          path: ~/.cache\n          restore-keys: |\n            mkdocs-material-\n\n      - name: Install documentation dependencies\n        run: python -m pip install mkdocs-material\n\n      - name: Deploy to GitHub Pages\n        run: mkdocs gh-deploy --force\n</code></pre> <p>GitHub Pages source</p> <p>After the first deployment, set GitHub Pages to publish from the <code>gh-pages</code> branch in the repository settings.</p>"},{"location":"publishing/#manual-publishing","title":"Manual Publishing","text":"<p>You can also publish manually from a local checkout:</p> <pre><code>python -m pip install mkdocs-material\nmkdocs gh-deploy --force\n</code></pre> <p>Use manual publishing only when you intentionally want to update the public documentation outside the normal CI process.</p>"},{"location":"quickstart/","title":"Quickstart","text":"<p>Use <code>odk doctor</code> first, then generate a project with <code>odk create</code>.</p> <pre><code>odk doctor\nodk create\n</code></pre> <p>If <code>odk doctor</code> reports that a newer ODK version is available, upgrade first:</p> <pre><code>odk upgrade\n</code></pre> <p>The create command asks for:</p> <ol> <li>Project name</li> <li>Project path</li> <li>Git repository</li> <li>Odoo source code path</li> <li>Odoo version</li> <li>Python version</li> <li>Docker preference</li> <li>Editor configuration preferences</li> </ol> <p>For local development, ODK writes root <code>odoo.conf</code> with an <code>addons_path</code> built from the Odoo source checkout and the generated project <code>addons/</code> directory. It also sets <code>admin_passwd = admin</code>, <code>db_user=odoo</code>, and <code>db_password=odoo</code>.</p> <p>For Docker development, ODK also writes <code>config/odoo.conf</code> for the container.</p>"},{"location":"release/","title":"Release","text":"<p>ODK uses <code>cargo-dist</code> to build binary releases for macOS, Linux, and Windows.</p>"},{"location":"release/#release-trigger","title":"Release Trigger","text":"<p>Releases are created from Git tags that start with <code>v</code>.</p> <pre><code>git tag v0.1.0\ngit push origin v0.1.0\n</code></pre> <p>The release workflow then:</p> <ol> <li>Runs formatting, Clippy, and unit tests.</li> <li>Builds release artifacts with <code>cargo-dist</code>.</li> <li>Uploads artifacts for each supported platform.</li> <li>Creates a GitHub Release with generated release notes.</li> </ol>"},{"location":"release/#supported-targets","title":"Supported Targets","text":"Platform Target macOS ARM64 <code>aarch64-apple-darwin</code> macOS x64 <code>x86_64-apple-darwin</code> Linux x64 <code>x86_64-unknown-linux-gnu</code> Linux ARM64 <code>aarch64-unknown-linux-gnu</code> Windows x64 <code>x86_64-pc-windows-msvc</code>"},{"location":"release/#preflight-checks","title":"Preflight Checks","text":"<p>Before creating a tag, run:</p> <pre><code>cargo fmt --all -- --check\ncargo clippy --all-targets --all-features -- -D warnings\ncargo test --all-features\n</code></pre> <p>The pull request workflow also runs <code>dist plan</code> so release configuration problems are caught before merge.</p> <p>Tags publish releases</p> <p>Pushing a matching <code>v*</code> tag is a publishing action. Only tag commits that are ready to become public releases.</p>"},{"location":"troubleshooting/","title":"Troubleshooting","text":""},{"location":"troubleshooting/#odk-is-not-found","title":"<code>odk</code> Is Not Found","text":"<p>Add Cargo bin to your shell path:</p> <pre><code>echo 'export PATH=\"$HOME/.cargo/bin:$PATH\"' &gt;&gt; ~/.zshrc\nsource ~/.zshrc\n</code></pre>"},{"location":"troubleshooting/#docker-files-were-not-generated","title":"Docker Files Were Not Generated","text":"<p>Run <code>odk create</code> again and answer <code>yes</code> when prompted for Docker.</p>"},{"location":"troubleshooting/#local-odooconf-has-the-wrong-addons-path","title":"Local <code>odoo.conf</code> Has The Wrong Addons Path","text":"<p>Install the latest local build:</p> <pre><code>cargo install --path . --locked\n</code></pre> <p>Then regenerate the project. The root <code>odoo.conf</code> should contain the Odoo source addons path and the generated project <code>addons/</code> path.</p>"},{"location":"upgrade/","title":"<code>odk upgrade</code>","text":"<p><code>odk upgrade</code> installs the latest ODK GitHub Release.</p> <pre><code>odk upgrade\n</code></pre> <p>The command:</p> <ol> <li>Checks the running ODK version.</li> <li>Checks the latest GitHub Release.</li> <li>Runs the release installer when an update is available.</li> </ol> <p>On macOS and Linux, it uses the latest shell installer:</p> <pre><code>curl --proto '=https' --tlsv1.2 -LsSf https://github.com/HanZawNyein/odoo_developer_kit/releases/latest/download/odoo-developer-kit-installer.sh | sh\n</code></pre> <p>On Windows, it uses the latest PowerShell installer.</p> <p><code>odk doctor</code> also checks for a newer ODK release and prints a warning when the installed version is behind.</p>"}]}