[project]
name = "{{ project_name }}"
version = "0.1.0"
description = "Odoo {{ odoo_version }} development project."
requires-python = ">={{ python_version }}"
dependencies = []

[dependency-groups]
dev = []
